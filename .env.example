PORT=3000
JWT_SECRET=please_change_this_secret
# Optional: separate secret for Aadhaar hashing (defaults to JWT_SECRET)
# AADHAAR_PEPPER=change_me_to_a_long_random_string

# Render / Linux: sqlite3 native module is rebuilt on install (see scripts/postinstall.js).
# To force rebuild locally: FORCE_SQLITE3_REBUILD=1 npm install
